using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public static class PlayerColor
{
    public static Color GetColor(PlayerColorType type)
    {
        return PlayerColor.GetColor(type);
    }
}
